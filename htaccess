<IfModule mod_rewrite.c>
    RewriteEngine On
    Options -Indexes

    # Redirect all incoming traffic to the public folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>